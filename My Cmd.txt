My Commands :-

1. Git

    git config --global user.name "_" : to set your name
    git config user.name : to see your name
    git config --global user.email "_"
    git config user.email "_"
    git init
    git add .
    git commit -m "_"
    git push
    git status
    git remote add origin "_"
    git push -u origin master
    git pull --rebase origin master
    git log 
     
    l

2.Linux

    Sing : $ = Regular User
    ls
    mkdir
    cd 
    cd ..
    pwd
    sudo apt-get update
    sudo apt-get upgrade
    ls -R 
    la -l
    ls -a : to see hidden files
    clear
    echo shashi : print "_"
    printf "my name is shashi\n"
    touch shashi.txt
    touch .shashi : hidden file
    touch SHASHI.txt
    ps : shows the processes
    ps -a : shows all bg processes
    top : shows running processes
